apiVersion: apps/v1
kind: Deployment
metadata:
  name: currency-converter
spec:
  template:
    spec:
      containers:
        - name: currency-converter
          env:
            - name: FLASK_ENV
              value: "production"

